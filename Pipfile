[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"
python-dotenv = "*"
flask-cors = "*"
flask-sqlalchemy = "*"

[dev-packages]

[requires]
python_version = "3.10"

[scripts]
init_db = "python init_db.py"
dev = "flask run -p ${PORT}"
start = "gunicorn server_app:app"
